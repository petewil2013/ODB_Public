/**
 * Google Analytics 4 bootstrap (external file so CSP can avoid script inline).
 * Measurement ID must match the async loader in index.html / photos.html.
 */
window.dataLayer = window.dataLayer || [];
function gtag() {
  dataLayer.push(arguments);
}
gtag('js', new Date());
gtag('config', 'G-D5C2E35JSS');
